export enum AgentQueryKeys {
  GetAgents = "GetAgents",
}

export enum MapQueryKeys {
  GetMaps = "GetMaps",
}
export enum WeaponQueryKeys {
  GetWeapons = "GetWeapons",
}

export enum Routes {
  Homepage = "/",
  Weapons = "/weapons",
  Agents = "/agents",
  Maps = "/maps",
}
